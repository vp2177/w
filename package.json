{
  "private": true,
  "scripts": {
    "build:swc": "spack",
    "build:es": "esbuild --bundle src/main.jsx --outdir=.gen --minify",
    "build:sass": "",
    "build": "npm run build:es",
    "format": "prettier -w .",
    "format:check": "prettier -c .",
    "type:check": "tsc"
  },
  "devDependencies": {
    "prettier": "2.8.0",
    "esbuild": "0.15.15",
    "typescript": "4.9.3",
    "sass": "1.56.1"
  },
  "dependencies": {
    "preact": "10.11.3",
    "hyperactiv": "0.11.3"
  },
  "oldDependencies": {
    "@swc/cli": "0.1.57: Crashy",
    "kefir": "",
    "inferno": "Doesn't have hooks",
    "solid-js": "1.6.2: Needs Babel. Missing typings? h() not JSX compatible",
    "omi": "<slot>ting broken when not using Shadow DOM"
  }
}
